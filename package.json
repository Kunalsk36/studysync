{
  "name": "studysync",
  "version": "1.0.0",
  "private": true,
  "description": "Shared root-level tooling for the StudySync monorepo (frontend/ + backend/).",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.4.2"
  }
}
